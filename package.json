{
  "name": "db2-proc",
  "version": "1.0.0",
  "description": "ML with DB2",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clientBuild": "cd client && ng build && cp -R node_modules ../build/",
    "start": "npm run clientBuild && nodemon app.js --exec babel-node --"
  },
  "author": "Sanjeev Ghimire",
  "license": "MIT",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "dotenv": "^8.0.0",
    "es6-promise": "^4.2.6",
    "express": "^4.17.1",
    "ibm_db": "^2.5.2"
  },
  "devDependencies": {
    "nodemon": "^1.19.1"
  }
}
